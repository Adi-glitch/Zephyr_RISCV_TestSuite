# Phase 0 is pre-MMU sanity: just ztest on the S-mode board (RISCV_S_MODE
# comes from the qemu .../smode board defconfig). No CONFIG_RISCV_MMU here —
# phases stay independent, like the bare-metal suite's.
CONFIG_ZTEST=y
